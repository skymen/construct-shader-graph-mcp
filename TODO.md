The MCP should have one command per actual command instead of a single generic call command.

The commands return way too much info and it overloads the context with irrelevant info.

Node Type list should not return all of the nodetype info, it should instead only return categories and names
